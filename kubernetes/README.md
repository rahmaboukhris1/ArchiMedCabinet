# Kubernetes

Manifests de déploiement pour Kubernetes.

Prêt (exemples) :
- Deployments : backend, frontend, mysql, redis
- Services : ClusterIP / NodePort / LoadBalancer
- Ingress : exposition HTTP et TLS
- ConfigMap / Secret : configuration externalisée

Utilisé pour staging et production.