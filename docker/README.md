# Docker

Fichiers de configuration pour la conteneurisation.

- `backend/` — Dockerfile PHP / Nginx
- `frontend/` — Dockerfile Node / Angular
- `mysql/` — configuration MySQL

Orchestration :
- `docker-compose.yml` (production)
- `docker-compose.local.yml` (développement local)